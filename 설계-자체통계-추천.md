# 자체 티어덱 통계 + 추천 엔진 설계 (Supabase · Next.js · Cloudflare)

작성 2026-09-17. 현재 코드(`lib/score.ts`, `lib/gen/*.json`)를 기준으로, 외부 크롤링 → **자체 수집·정제·집계** 로 넘어갈 때의 설계안.

---

## 0. 한 장 요약

```
[수집]  Riot Match-V5 (또는 지금처럼 metatft 스냅샷)  ──▶  raw.matches / raw.participants
[정제]  참가자 보드 → 특성 시그니처 → 덱 클러스터           ──▶  decks, deck_units, deck_items, deck_levels
[집계]  SQL 뷰/머티리얼라이즈드 뷰                          ──▶  deck_stats (avg, top4, win, pick, games)
[서빙]  /api/recommend (Edge)  =  Supabase에서 패치 덱 1회 로드(캐시) + JS로 점수 계산
```

- **점수 계산은 JS(API route)에서.** 덱은 패치당 수십 개, 입력은 1명분 — SQL로 할 이유가 없고, 지금 `lib/score.ts` 로직을 그대로 옮기면 된다.
- **집계(통계)는 SQL에서.** 수십만 판을 훑는 건 DB 일. 머티리얼라이즈드 뷰 + 크론 리프레시.
- Edge에서 supabase-js는 문제없음(fetch 기반). `pg`·Node API 쓰는 라이브러리만 피하면 됨.

---

## 1. 스키마

전부 `patch_id`에 종속. 마스터(units/items/traits)도 패치마다 스탯·시너지가 바뀌므로 패치별 행으로 둔다(용량 무시해도 될 크기).

```sql
-- =========================================================
-- 0. 패치
-- =========================================================
create table patches (
  id          serial primary key,
  set_number  int  not null,                 -- 18
  version     text not null,                 -- '16.18', 'pbe'
  label       text,                          -- '패치 18.2'
  is_current  boolean not null default false,
  started_at  date,
  created_at  timestamptz not null default now(),
  unique (set_number, version)
);
create unique index patches_one_current on patches (is_current) where is_current;

-- =========================================================
-- 1. 마스터 데이터 (CommunityDragon → sync)
-- =========================================================
create table traits (
  id          text not null,                 -- cdragon apiName: 'DA_18_Lunar'
  patch_id    int  not null references patches(id) on delete cascade,
  name        text not null,                 -- '달빛'
  img         text,
  breakpoints int[] not null default '{}',   -- {2,3,4,5}
  primary key (id, patch_id)
);

create table units (
  id          text not null,                 -- 'aphelios' (cdragon 캐릭터 폴더명)
  patch_id    int  not null references patches(id) on delete cascade,
  api_name    text not null,                 -- 'DA_18_Aphelios'  (매치 데이터의 character_id 와 매칭 키)
  name        text not null,
  cost        smallint not null check (cost between 1 and 5),
  img         text,
  planner_code text,                         -- 팀 플래너 코드 3hex
  primary key (id, patch_id)
);

create table unit_traits (
  unit_id   text not null,
  trait_id  text not null,
  patch_id  int  not null,
  primary key (unit_id, trait_id, patch_id),
  foreign key (unit_id, patch_id)  references units(id, patch_id)  on delete cascade,
  foreign key (trait_id, patch_id) references traits(id, patch_id) on delete cascade
);

create type item_kind as enum ('component', 'completed', 'emblem', 'artifact', 'radiant', 'support');

create table items (
  id          text not null,                 -- 'infinityedge' (cdragon apiName 소문자)
  patch_id    int  not null references patches(id) on delete cascade,
  api_name    text not null,                 -- 'TFT_Item_InfinityEdge'
  name        text not null,
  kind        item_kind not null,
  recipe      text[] ,                       -- completed/emblem 만: {'bf','gloves'}  (component id 2개)
  img         text,
  primary key (id, patch_id)
);

-- =========================================================
-- 2. 원천 데이터 (Riot Match-V5). 정제·집계의 입력. 서빙엔 안 씀.
-- =========================================================
create schema raw;

create table raw.matches (
  match_id     text primary key,             -- 'KR_1234567890'
  patch_id     int  not null references patches(id),
  game_version text not null,                -- 'Version 16.18.123 ...'
  queue_id     int  not null,                -- 1100 랭크
  played_at    timestamptz not null,
  fetched_at   timestamptz not null default now()
);

create table raw.participants (
  match_id     text not null references raw.matches(match_id) on delete cascade,
  puuid        text not null,
  placement    smallint not null check (placement between 1 and 8),
  level        smallint not null,
  last_round   smallint,
  tier         text,                         -- 'DIAMOND' (랭크 필터용)
  units        jsonb not null,               -- [{character_id, tier(성), items:[apiName...]}]
  traits       jsonb not null,               -- [{name, num_units, tier_current}]
  augments     text[] not null default '{}',
  primary key (match_id, puuid)
);
create index participants_patch_idx on raw.participants (match_id);
-- 레벨별 조합을 얻으려면 라운드별 스냅샷이 필요한데 Match-V5엔 최종 보드만 있음.
-- → 레벨별 조합은 (a) metatft처럼 최종 레벨이 N인 참가자의 보드를 "N렙 조합"으로 근사, 또는 (b) 인게임 앱 수집. 아래 deck_levels 참고.

-- =========================================================
-- 3. 정제 결과: 덱 (클러스터)
-- =========================================================
create table decks (
  id            text not null,               -- 'lunar-aphelios-nidalee' (슬러그, 패치 안에서 고정)
  patch_id      int  not null references patches(id) on delete cascade,
  name          text not null,               -- 자동 생성 이름
  display_name  text,                        -- 수동 보정 (deck-notes 역할)
  playstyle     text,
  carry_unit_id text not null,
  levelling     text,                        -- 'Fast 8' | 'Fast 9' | 'lvl 7' | 'Standard'
  difficulty    text,                        -- '쉬움' | '보통' | '어려움'
  signature     jsonb not null,              -- 클러스터 시그니처 {traits:{...}, units:{...}} (재현·디버깅용)
  is_published  boolean not null default true,
  primary key (id, patch_id),
  foreign key (carry_unit_id, patch_id) references units(id, patch_id)
);

-- 최종 조합 (배치 포함)
create table deck_units (
  deck_id     text not null,
  patch_id    int  not null,
  unit_id     text not null,
  target_star smallint not null default 2 check (target_star between 1 and 3),
  pos_row     smallint check (pos_row between 0 and 3),   -- 0 = 최전방
  pos_col     smallint check (pos_col between 0 and 6),
  is_core     boolean not null default true,              -- false = 유동 슬롯
  primary key (deck_id, patch_id, unit_id),
  foreign key (deck_id, patch_id) references decks(id, patch_id) on delete cascade,
  foreign key (unit_id, patch_id) references units(id, patch_id)
);

-- 아이템 배치. priority 1 = 1순위 빌드, 2~ = 대체.  role 로 "필수(캐리 몰아주기)" vs "유동" 구분
create type item_role as enum ('carry_core', 'carry_flex', 'tank', 'utility');
create table deck_items (
  deck_id   text not null,
  patch_id  int  not null,
  unit_id   text not null,
  item_id   text not null,
  role      item_role not null,
  priority  smallint not null default 1,
  pick_rate numeric(5,4),                    -- 그 유닛이 이 아이템을 든 비율 (집계에서)
  primary key (deck_id, patch_id, unit_id, item_id),
  foreign key (deck_id, patch_id) references decks(id, patch_id) on delete cascade,
  foreign key (item_id, patch_id) references items(id, patch_id)
);

-- 레벨별 최빈 조합 (4~10). 추천 엔진의 핵심 입력.
create table deck_levels (
  deck_id   text not null,
  patch_id  int  not null,
  level     smallint not null check (level between 3 and 11),
  unit_ids  text[] not null,
  avg_place numeric(4,2),
  games     int not null default 0,
  primary key (deck_id, patch_id, level),
  foreign key (deck_id, patch_id) references decks(id, patch_id) on delete cascade
);

-- 덱별 추천 증강 / 상성 (선택)
create table deck_augments (
  deck_id text not null, patch_id int not null, augment_id text not null,
  tier text not null,                        -- 'S' | 'A' | 'B'
  avg_place numeric(4,2), games int,
  primary key (deck_id, patch_id, augment_id),
  foreign key (deck_id, patch_id) references decks(id, patch_id) on delete cascade
);
create table deck_counters (
  deck_id text not null, patch_id int not null, against_deck_id text not null,
  place_change numeric(4,2) not null,        -- + = 같은 로비에 있을 때 내 등수가 나빠짐
  primary key (deck_id, patch_id, against_deck_id),
  foreign key (deck_id, patch_id) references decks(id, patch_id) on delete cascade
);

-- 참가자 ↔ 덱 매핑 (정제 단계 산출). 이게 있어야 통계를 SQL로 뽑는다.
create table participant_decks (
  match_id  text not null,
  puuid     text not null,
  patch_id  int  not null,
  deck_id   text not null,
  distance  real not null,                   -- 클러스터 중심과의 거리 (품질 필터)
  primary key (match_id, puuid),
  foreign key (match_id, puuid) references raw.participants(match_id, puuid) on delete cascade,
  foreign key (deck_id, patch_id) references decks(id, patch_id) on delete cascade
);
create index participant_decks_deck_idx on participant_decks (patch_id, deck_id);

-- =========================================================
-- 4. 집계: 통계는 SQL이 한다
-- =========================================================
create materialized view deck_stats as
select
  pd.patch_id,
  pd.deck_id,
  count(*)                                             as games,
  round(avg(p.placement)::numeric, 2)                  as avg_place,
  round(avg((p.placement = 1)::int)::numeric, 4)       as win_rate,
  round(avg((p.placement <= 4)::int)::numeric, 4)      as top4_rate,
  round(count(*)::numeric
        / nullif((select count(*) from raw.participants rp join raw.matches rm using (match_id) where rm.patch_id = pd.patch_id), 0), 4)
                                                       as pick_rate,
  case
    when avg(p.placement) < 4.00 then 'OP'
    when avg(p.placement) < 4.25 then 'S'
    when avg(p.placement) < 4.50 then 'A'
    when avg(p.placement) < 4.75 then 'B'
    else 'C' end                                       as tier_label
from participant_decks pd
join raw.participants p using (match_id, puuid)
group by pd.patch_id, pd.deck_id;
create unique index deck_stats_pk on deck_stats (patch_id, deck_id);
-- 갱신: refresh materialized view concurrently deck_stats;  (pg_cron 으로 1시간마다)

-- 일자별 추세 (스파크라인)
create materialized view deck_trends as
select pd.patch_id, pd.deck_id, date_trunc('day', m.played_at)::date as day,
       count(*) as games, round(avg(p.placement)::numeric, 2) as avg_place
from participant_decks pd
join raw.participants p using (match_id, puuid)
join raw.matches m using (match_id)
group by 1, 2, 3;
create unique index deck_trends_pk on deck_trends (patch_id, deck_id, day);

-- 서빙용 한 방 뷰: 덱 + 통계 + 유닛/아이템/레벨을 JSON으로 (API가 이걸 1번 읽고 캐시)
create view decks_bundle as
select
  d.patch_id, d.id, coalesce(d.display_name, d.name) as name, d.playstyle, d.carry_unit_id,
  d.levelling, d.difficulty,
  s.games, s.avg_place, s.win_rate, s.top4_rate, s.pick_rate, s.tier_label,
  (select jsonb_agg(jsonb_build_object('unitId', du.unit_id, 'star', du.target_star, 'pos', case when du.pos_row is null then null else jsonb_build_array(du.pos_row, du.pos_col) end, 'core', du.is_core))
     from deck_units du where du.deck_id = d.id and du.patch_id = d.patch_id)         as units,
  (select jsonb_agg(jsonb_build_object('unitId', di.unit_id, 'itemId', di.item_id, 'role', di.role, 'priority', di.priority) order by di.priority)
     from deck_items di where di.deck_id = d.id and di.patch_id = d.patch_id)         as items,
  (select jsonb_object_agg(dl.level, jsonb_build_object('units', dl.unit_ids, 'avg', dl.avg_place, 'count', dl.games))
     from deck_levels dl where dl.deck_id = d.id and dl.patch_id = d.patch_id)        as levels,
  (select jsonb_agg(jsonb_build_object('deckId', dc.against_deck_id, 'placeChange', dc.place_change))
     from deck_counters dc where dc.deck_id = d.id and dc.patch_id = d.patch_id)      as counters
from decks d
left join deck_stats s on s.patch_id = d.patch_id and s.deck_id = d.id
where d.is_published;

-- =========================================================
-- 5. RLS: 전부 공개 읽기, 쓰기는 service role 만
-- =========================================================
alter table decks enable row level security;
create policy "public read" on decks for select using (true);
-- (units, items, traits, deck_* , deck_stats 동일. raw.* 는 정책 없이 service role 전용)
```

### 정제(클러스터링) 메모 — `raw.participants → decks`
metatft 방식의 축약판. 패치 초기엔 수동 시드도 가능.
1. 참가자 보드 → 벡터: 유닛 one-hot(성 가중) + 활성 특성 단계. 이게 `decks.signature`.
2. 최종 레벨 8 이상, 등수 상관없이 전부 포함(피크 왜곡 방지). k-means 또는 "시그니처 상위 N 유닛 동일" 그룹핑. 표본 200판 미만 클러스터는 버림.
3. 클러스터 대표: 유닛 등장률 ≥ 60% → `deck_units`(is_core), 30~60% → 유동. 성은 최빈값. 배치는 셀별 최빈(충돌 시 2순위).
4. 아이템: 클러스터 안에서 유닛별 3템 조합 빈도 → priority. **캐리 판정** = 완성템 3개 든 비율 가장 높은 유닛(동률이면 고코스트). 캐리 아이템 중 등장률 ≥ 50% → `carry_core`, 나머지 `carry_flex`. 캐리 아닌 유닛의 방어템 → `tank`.
5. 레벨별 조합: 최종 레벨이 N(4~9)인 **같은 클러스터** 참가자의 보드를 N렙 조합으로 근사. (Match-V5엔 라운드 스냅샷이 없음. 정확한 레벨링은 나중에 인게임 수집으로 대체.)
6. 이름: 최고 단계 특성 1 + 캐리 1~2 → `name`. `display_name`으로 수동 보정.

---

## 2. 추천(매칭) 로직

### 2.1 입력

```ts
type RecommendInput = {
  patch?: string;                       // 없으면 is_current
  stage?: string;                       // '2-1' … '5-1'  → 레벨 창(window) 결정
  components: ComponentId[];            // 재료 (bf, bow, …) 최대 8
  completed: string[];                  // 완성 아이템 id (분해 불가로 취급)
  units: { unitId: string; star: 1|2|3 }[];
  rivals?: string[];                    // 로비 스카우팅: 상대 보드 유닛 (선택)
};
```

### 2.2 가중치 — 지금 `lib/score.ts` 그대로, 라운드 반영만 강화

| 축 | 만점 | 계산 | 라운드 영향 |
|---|---|---|---|
| **조합 일치** | 40 | 스테이지에 대응하는 레벨 창 `[L-1, L, L+1]`의 `deck_levels` 조합과 성 가중(1/1.5/3) 일치율. 창 안 최고값. 캡 1.25 | **창 폭**: 2-x 는 ±1 넓게(후보 넓힘), 4-x 이후는 L과 최종만(좁힘). 5-1 이후엔 최종 조합 가중 ×1.3 |
| **아이템 방향** | 25 | 캐리 아이템 레시피의 재료 멀티셋 vs 내 재료. `carry_core`는 가중 2, `carry_flex`는 1, `tank`는 0.5. 완성템은 **정확히 일치할 때만** 재료 2개로 인정(분해 불가) | 4-x 이후 완성템 미일치는 감점 0.9× (그 시점엔 이미 굳어서) |
| **캐리 보유** | 15 | 캐리 있으면 `15×(0.6+0.4×starRatio)`, 없어도 0(감점 없음) | 4-5 이후 4~5코 캐리 미보유는 0.85× (늦음) |
| **메타** | 20 | `(4.5 − avg_place)/2 × 20`, 접점 없으면 0.3× | 초반일수록 메타 비중↑: 2-x 는 ×1.2 (정보 없을 땐 통계 따라감) |
| 3성 규칙 | +12 / ×0.5 | 3성 기물을 최종 조합에 쓰면 +12, 안 쓰면 총점 반감 | — |
| 로비 경합 | ×(0.5~1) | 상대 유닛과 핵심 유닛 겹침 비율, 상대 덱 추정 후 `deck_counters` 상성 | — |

**스테이지 → 레벨 매핑** (metatft 레벨 통계 기준): `2-1~2-4→4, 2-5~3-1→5, 3-2~3-5→6, 3-6~4-1→7, 4-2~5-6→8, 6-1+→9`

**"넓게 → 좁게"의 실제 구현**: 결과 개수도 스테이지로 조절 — 2-x: 상위 5 (분기 트리와 함께), 3-x: 3, 4-x 이후: 2. 그리고 스테이지가 늦을수록 `조합 일치` 점수의 **1위와 2위 격차를 증폭**(softmax 온도 ↓)해서 한 덱으로 수렴하게.

### 2.3 TypeScript 구현 (핵심만; `lib/score.ts`에서 이식)

```ts
// lib/engine/score.ts — 순수 함수. DB도, Next도 모름. Edge/Node/브라우저 어디서나.
export const STAR_MULT = { 1: 1, 2: 1.5, 3: 3 } as const;
const ROLE_W = { carry_core: 2, carry_flex: 1, tank: 0.5, utility: 0 } as const;

export function stageLevel(stage?: string) {
  if (!stage) return null;
  const [s, r] = stage.split("-").map(Number); const t = s * 10 + r;
  return t < 25 ? 4 : t < 32 ? 5 : t < 36 ? 6 : t < 42 ? 7 : t < 61 ? 8 : 9;
}
/** 라운드별 창 폭·수렴 정도 */
export function stageProfile(stage?: string) {
  const L = stageLevel(stage);
  if (L == null) return { L: null, window: [4, 7], includeFinal: true, finalW: 1, metaW: 1, temp: 1, topN: 3 };
  if (L <= 5)     return { L, window: [L - 1, L + 1], includeFinal: false, finalW: 1,   metaW: 1.2, temp: 1.0, topN: 5 };
  if (L <= 7)     return { L, window: [L, L + 1],     includeFinal: true,  finalW: 1,   metaW: 1.0, temp: 0.8, topN: 3 };
  return                 { L, window: [L, L],         includeFinal: true,  finalW: 1.3, metaW: 0.9, temp: 0.6, topN: 2 };
}

export function scoreDeck(input: RecommendInput, deck: DeckBundle, ctx: { itemRecipe: (id: string) => string[] | undefined; unitCost: (id: string) => number }) {
  const p = stageProfile(input.stage);
  const starOf = new Map(input.units.map((u) => [u.unitId, u.star]));

  // 1) 조합 일치 (0~40)
  const comps: { label: string; units: string[]; w: number }[] = [];
  for (const [lv, c] of Object.entries(deck.levels ?? {}))
    if (+lv >= p.window[0] && +lv <= p.window[1] && c.units.length) comps.push({ label: `${lv}렙`, units: c.units, w: 1 });
  if (p.includeFinal) comps.push({ label: "최종", units: deck.units.map((u) => u.unitId), w: p.finalW });
  let best = { ratio: 0, hit: [] as string[], label: "" };
  for (const c of comps) {
    const hit = c.units.filter((u) => starOf.has(u));
    const weighted = hit.reduce((s, u) => s + STAR_MULT[starOf.get(u)!], 0);
    const ratio = Math.min(weighted / c.units.length, 1.25) * c.w;
    if (ratio > best.ratio) best = { ratio, hit, label: c.label };
  }
  const earlyScore = Math.min(best.ratio, 1.25) * 40;

  // 2) 아이템 방향 (0~25) — 역할 가중, 완성템은 정확 일치만
  const carryItems = deck.items.filter((i) => i.unitId === deck.carryUnitId && i.priority === 1);
  const need = new Map<string, number>(); let needTotal = 0;
  for (const it of carryItems) for (const c of ctx.itemRecipe(it.itemId) ?? []) { const w = ROLE_W[it.role]; need.set(c, (need.get(c) ?? 0) + w); needTotal += w; }
  let matched = 0;
  const done = [...input.completed];
  for (const it of carryItems) { const k = done.indexOf(it.itemId); if (k >= 0) { done.splice(k, 1); matched += 2 * ROLE_W[it.role]; for (const c of ctx.itemRecipe(it.itemId) ?? []) need.set(c, (need.get(c) ?? 0) - ROLE_W[it.role]); } }
  for (const c of input.components) { const n = need.get(c) ?? 0; if (n > 0) { need.set(c, n - 1); matched += 1; } }
  const itemScore = needTotal ? Math.min(matched / needTotal, 1) * 25 : 0;

  // 3) 캐리 보유 (0~15)
  const carry = input.units.find((u) => u.unitId === deck.carryUnitId);
  const target = deck.units.find((u) => u.unitId === deck.carryUnitId)?.star ?? 2;
  let carryScore = carry ? Math.min(15, 15 * (0.6 + 0.4 * Math.min(STAR_MULT[carry.star] / STAR_MULT[target], 2))) : 0;
  if (!carry && p.L && p.L >= 8 && ctx.unitCost(deck.carryUnitId) >= 4) carryScore *= 0.85;

  // 4) 메타 (0~20)
  const metaBase = Math.max(0, Math.min(20, ((4.5 - deck.avgPlace) / 2) * 20)) * p.metaW;
  const hasConnection = best.hit.length > 0 || matched > 0;
  const metaScore = hasConnection ? metaBase : metaBase * 0.3;

  // 5) 3성 규칙, 6) 로비 경합 — 현재 lib/score.ts 그대로
  let mult = 1, bonus = 0;
  for (const u of input.units.filter((u) => u.star === 3)) {
    if (u.unitId === deck.carryUnitId) continue;
    if (deck.units.some((d) => d.unitId === u.unitId)) bonus += 12; else mult *= 0.5;
  }
  if (input.rivals?.length) {
    const core = deck.units.filter((u) => u.core).map((u) => u.unitId);
    const contested = core.filter((u) => input.rivals!.includes(u) && !starOf.has(u));
    if (contested.length) mult *= 1 - Math.min(0.5, (contested.length / core.length) * 1.2);
  }

  const total = Math.min(100, (earlyScore + itemScore + carryScore + metaScore + bonus) * mult);
  return { total, earlyScore, itemScore, carryScore, metaScore, bestLabel: best.label };
}

/** 라운드가 늦을수록 1위로 수렴 (softmax 온도) → UI의 % 는 이걸로 */
export function recommend(input: RecommendInput, decks: DeckBundle[], ctx: Ctx) {
  const p = stageProfile(input.stage);
  const scored = decks.map((deck) => ({ deck, s: scoreDeck(input, deck, ctx) })).sort((a, b) => b.s.total - a.s.total);
  const max = scored[0]?.s.total ?? 1;
  return scored.slice(0, p.topN).map((x) => ({
    ...x, confidence: Math.round(100 * Math.exp((x.s.total - max) / (25 * p.temp))),   // 1위 = 100, 격차는 온도로
  }));
}
```

### 2.4 SQL vs JS — 어디서 계산할까

| | SQL (Postgres 함수/뷰) | JS (API route) |
|---|---|---|
| 데이터 크기 | 패치당 덱 20~60개, 레벨 7개, 아이템 30개 → **전부 합쳐 50KB** | 같은 데이터를 한 번 읽어 메모리에 |
| 로직 복잡도 | 성 가중·멀티셋 매칭·softmax·3성 규칙 → PL/pgSQL로 쓰면 길고 디버깅 고통 | 이미 TS로 있음 (`lib/score.ts`), 단위 테스트 쉬움 |
| 지연 | 요청마다 DB 왕복 1회 + 함수 실행 | 덱 번들을 **캐시**(Edge Cache/KV, 패치당 1개)하면 DB 왕복 0회 |
| 배포 | 마이그레이션으로 함수 버전 관리 | 코드 배포 |
| 재사용 | 앱 밖에서도 호출 가능 | 프런트에서도 같은 함수 사용 가능 (홈 회전목마 계산 등) |

**추천: JS.** 이유는 단순 — 입력 1명 × 덱 수십 개는 계산이 아니라 조회 수준이고, 이미 검증된 TS 로직이 있다. SQL은 **집계**(deck_stats, deck_trends, 클러스터 대표 뽑기)에 쓴다. 나중에 "모든 유저 입력 로그로 추천 품질 측정" 같은 걸 하게 되면 그때 SQL 쪽으로 옮길 후보가 생긴다.

---

## 3. API route — `/api/recommend`

```ts
// app/api/recommend/route.ts
export const runtime = "edge";               // next-on-pages 계열. OpenNext(Workers) 쓰면 이 줄 불필요 (4장 참고)

import { createClient } from "@supabase/supabase-js";
import { recommend, type RecommendInput } from "@/lib/engine/score";

const MAX = { components: 8, completed: 4, units: 12, rivals: 20 };
const VALID_COMPONENT = new Set(["bf", "bow", "rod", "tear", "vest", "cloak", "belt", "gloves", "spatula", "pan"]);

function supabase() {
  // Edge: fetch 기반 클라이언트만. anon key + RLS 공개 읽기.
  return createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!, {
    auth: { persistSession: false, autoRefreshToken: false },
    global: { fetch },                       // Workers의 fetch 사용 (기본값이지만 명시)
  });
}

/** 패치별 덱 번들 — 1시간 캐시 (Cloudflare Cache API). 패치 바뀌면 키가 바뀜 */
async function loadBundle(patch: string) {
  const cacheKey = new Request(`https://cache.internal/bundle/${patch}`);
  const cache = (globalThis as any).caches?.default as Cache | undefined;   // Workers 전용, 로컬 dev엔 없음
  const hit = await cache?.match(cacheKey);
  if (hit) return hit.json();

  const sb = supabase();
  const { data: p } = patch === "current"
    ? await sb.from("patches").select("id, version").eq("is_current", true).single()
    : await sb.from("patches").select("id, version").eq("version", patch).single();
  if (!p) throw new Error("patch not found");

  const [decks, items, units] = await Promise.all([
    sb.from("decks_bundle").select("*").eq("patch_id", p.id),
    sb.from("items").select("id, recipe, kind").eq("patch_id", p.id),
    sb.from("units").select("id, cost").eq("patch_id", p.id),
  ]);
  const bundle = { patch: p.version, decks: decks.data ?? [], items: items.data ?? [], units: units.data ?? [] };
  await cache?.put(cacheKey, new Response(JSON.stringify(bundle), { headers: { "cache-control": "max-age=3600" } }));
  return bundle;
}

function parse(body: unknown): RecommendInput {
  const b = (body ?? {}) as Partial<RecommendInput>;
  return {
    patch: typeof b.patch === "string" ? b.patch : "current",
    stage: typeof b.stage === "string" && /^[2-7]-[1-7]$/.test(b.stage) ? b.stage : undefined,
    components: (b.components ?? []).filter((c) => VALID_COMPONENT.has(c)).slice(0, MAX.components),
    completed: (b.completed ?? []).filter((s) => typeof s === "string").slice(0, MAX.completed),
    units: (b.units ?? []).filter((u) => u && typeof u.unitId === "string").map((u) => ({ unitId: u.unitId, star: ([1, 2, 3].includes(u.star) ? u.star : 1) as 1 | 2 | 3 })).slice(0, MAX.units),
    rivals: (b.rivals ?? []).filter((s) => typeof s === "string").slice(0, MAX.rivals),
  };
}

export async function POST(req: Request) {
  let input: RecommendInput;
  try { input = parse(await req.json()); } catch { return Response.json({ error: "bad json" }, { status: 400 }); }

  const bundle = await loadBundle(input.patch ?? "current");
  const recipe = new Map(bundle.items.map((i: any) => [i.id, i.recipe as string[] | null]));
  const cost = new Map(bundle.units.map((u: any) => [u.id, u.cost as number]));
  // 입력 유효성: 이 패치에 없는 id 는 버림 (에러 대신 무시 — 클라이언트 캐시가 한 패치 늦을 수 있음)
  input.units = input.units.filter((u) => cost.has(u.unitId));
  input.completed = input.completed.filter((id) => recipe.has(id));

  const results = recommend(input, bundle.decks, { itemRecipe: (id) => recipe.get(id) ?? undefined, unitCost: (id) => cost.get(id) ?? 1 });
  return Response.json(
    { patch: bundle.patch, stage: input.stage ?? null, results: results.map((r) => ({ deckId: r.deck.id, name: r.deck.name, confidence: r.confidence, score: r.s })) },
    { headers: { "cache-control": "no-store" } },   // 입력마다 다름. 번들 캐시로 충분
  );
}
```

요청/응답 예:
```http
POST /api/recommend
{ "stage": "2-5", "components": ["bf","cloak"], "completed": [], "units": [{"unitId":"ornn","star":2},{"unitId":"xayah","star":2}] }

200
{ "patch": "16.18", "stage": "2-5",
  "results": [
    { "deckId": "elderwood-aphelios", "name": "나무정령 아펠리오스", "confidence": 100, "score": { "total": 47.1, ... } },
    { "deckId": "highvalue-draven",   "name": "고밸류 드레이븐",     "confidence": 88,  "score": { ... } } ] }
```

---

## 4. Cloudflare Edge 호환 — 되나, 뭘 조심하나

**됩니다.** 단, 어댑터 선택이 먼저다.

| | `@cloudflare/next-on-pages` (1.13.x) | `@opennextjs/cloudflare` (1.20.x) |
|---|---|---|
| 상태 | **유지보수 모드** — Cloudflare가 OpenNext로 옮기라고 안내 | Cloudflare 공식 권장. Workers(+Static Assets)에 배포, Pages도 가능 |
| 런타임 | 모든 동적 라우트에 `export const runtime = 'edge'` **필수** | Node 런타임 그대로(workerd의 `nodejs_compat`). `runtime='edge'` 불필요, 오히려 제거 |
| Next 16 | 15까지 검증, 16은 미보장 | 15/16 지원 |
| 결론 | 신규면 안 씀 | **이걸로.** `npx create-cloudflare` 또는 기존 프로젝트에 `@opennextjs/cloudflare` 추가, `wrangler.jsonc` + `open-next.config.ts` |

어느 쪽이든 공통 주의사항:

1. **supabase-js는 OK.** `@supabase/supabase-js` 2.x는 `fetch` 기반, Node 전용 API 없음. `pg`, `postgres`(porsager), Prisma(엔진) 는 **안 됨** — Prisma를 꼭 써야 하면 Accelerate/Data Proxy. 우린 supabase-js면 충분.
2. **환경변수**: Workers는 `process.env`가 아니라 바인딩. OpenNext는 `process.env`를 채워주지만, `NEXT_PUBLIC_*` 외 비밀(service role key)은 `wrangler secret put`으로. 빌드 타임 인라인이 아니라 런타임 바인딩이라 `process.env.X`를 **모듈 최상단에서 읽지 말고 핸들러 안에서** 읽기.
3. **캐시**: `caches.default`(Cache API)는 Workers에서만 존재, `next dev`엔 없음 → 위 코드처럼 옵셔널 처리. 번들처럼 패치당 1개인 건 **KV**가 더 단순(`env.KV.get('bundle:16.18')`). ISR(`revalidate`)은 OpenNext에서 R2/KV 인크리멘탈 캐시 설정이 필요 — 초기엔 Cache API/KV 직접 쓰는 게 덜 헤맴.
4. **번들 크기**: Workers 무료 3MB(gzip), 유료 10MB. `lib/gen/*.json`을 서버 번들에 넣지 말고(지금은 넣고 있음) DB/KV에서 읽기. 클라이언트 번들도 마찬가지 — 홈의 회전목마 계산 같은 건 API로.
5. **시간 제한**: Workers CPU 시간 무료 10ms/요청(유료 30s wall). 덱 60개 × 점수 계산은 1ms 미만. DB 왕복이 병목이므로 번들 캐시 필수.
6. **Node API**: `fs`, `path`, `crypto.createHash` 등 사용 금지. `crypto.subtle`, `TextEncoder`는 OK. `next/font/google`은 빌드 타임이라 OK. `sharp`(next/image 최적화)는 안 됨 → `images.unoptimized: true` 또는 Cloudflare Images.
7. **정적 페이지 우선**: `/decks`, `/champions`, `/items`, `/traits`, `/augments`, `/deck/[id]`(쿼리 없을 때)는 빌드 타임 프리렌더(`generateStaticParams`)로 → Workers 호출 0. 지금 Netlify에서 함수 호출 초과로 503 난 것과 같은 문제를 처음부터 피함. 유저 상태(보유 토글)는 URL 쿼리 대신 클라이언트 상태로 옮기고, 점수만 `/api/recommend`로.
8. **Supabase 리전**: Cloudflare 엣지는 전 세계인데 Supabase는 한 리전(서울) → 한국 유저는 빠르고 해외는 DB 왕복이 길다. 번들 캐시가 있으면 체감 없음.

---

## 5. 지금 코드에서 옮기는 순서

1. **스키마 적용** — 1장 SQL을 Supabase 마이그레이션으로. `patches`에 현재 패치 1행.
2. **시드 스크립트** — `scripts/sync.mjs`(cdragon) → `units/items/traits` upsert. `scripts/sync-meta.mjs`(metatft) → `decks/deck_units/deck_items/deck_levels/deck_counters` upsert + `deck_stats` 대신 임시 `deck_stats_manual` 테이블. 즉 **수집 소스가 바뀔 때까진 metatft 스냅샷을 DB에 넣어 서빙만 먼저 전환.**
3. **엔진 분리** — `lib/score.ts` → `lib/engine/score.ts` (순수 함수, `lib/data.ts` 의존 제거, ctx 주입). 기존 테스트 시나리오(오른·자야·바루스 등)로 회귀 확인.
4. **API route** + 프런트가 API 호출. 홈 localStorage 상태는 그대로.
5. **OpenNext로 Cloudflare 배포**. 정적 페이지 프리렌더 전환.
6. **Riot Match-V5 수집기** (Cron Worker 또는 Supabase Edge Function): 챌린저~다이아 상위 puuid 시드 → match id → participants 적재. 하루 1~2만 판이면 충분.
7. **클러스터링 잡** (Python/TS, 하루 1회) → `decks`/`participant_decks` 갱신 → `refresh materialized view`. 이 시점에 metatft 의존 제거.

Riot API 주의: 개발 키는 24시간, 프로덕션 키는 신청 필요(서비스 설명·URL). 레이트 리밋 20req/s·100req/2min(개발 키 기준). 매치 상세는 `match-v5/matches/{id}` 1판 = 1요청이라 하루 2만 판 = 약 3시간.
