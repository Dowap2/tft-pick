# 벤치마크: MetaTFT · OP.GG · 롤체지지(DAK.GG) vs TFT PICK

작성 2026-09-16. 세 사이트의 덱(메타) 페이지를 직접 열어보고 정리. 우리 현재 상태 기준으로 **개선 / 추가 / 독자 포인트** 3파트.

> 관찰 요약
> - **MetaTFT**: 통계 중심. 필터(큐·패치·기간·티어), 정렬, 덱마다 평균등수·픽률·승률·Top4, 태그(Fast 8/9·lvl 7 / Easy·Medium·Hard), 특성 아이콘 줄, 3성 표시, 클릭하면 배치·레벨링·옵션·초반 보드·증강·카운터. Early Comps / Pro Comps / Double Up 탭. 인게임 앱(라이브 오버레이)이 진짜 차별점.
> - **롤체지지(DAK.GG)**: 한국 친화. 덱마다 **팀 코드 복사**, "공략 더 보기"(사람이 쓴 가이드), HOT 태그, 시너지 아이콘 줄, 추천 메타 / 메타 통계 / 최근 1위 덱 탭, 챔피언·시너지·아이템·증강체·수호령/보상표·배치툴·게임 가이드·게시판. 광고 매우 많음.
> - **OP.GG**: 메타 트렌드 / 티어 리스트 / 유저 트렌드 / 랭킹 / 배치툴 / 버프·너프 / 게임 가이드 / 강의. 덱 상세에 초·중·후반 배치, 증강 추천, 아이템 우선순위. (헤드리스 접근 차단으로 스크린샷 못 뜸, 기존 지식 기준)
> - **셋의 공통점**: 전부 "완성된 덱 목록"을 보여주고, **내 현재 상황을 입력받는 곳은 없다**. 인게임 앱(MetaTFT)만 예외.

---

## 1. 개선하면 좋을 부분 (지금 있는 기능을 그들 수준으로)

| # | 항목 | 그들은 | 우리는 | 개선안 | 데이터 | 난이도 |
|---|---|---|---|---|---|---|
| 1-1 | **덱 통계 4종** | 평균등수 · 픽률 · 승률(1등) · Top4 | 평균등수 · 게임 수만 | metatft `comp_details.placements`/`players`로 1등률·Top4율 계산, 픽률 = 게임수/전체. 카드 우측 숫자 4개로 | 있음 | 낮음 |
| 1-2 | **덱 태그** | `Fast 8` `Fast 9` `lvl 7 리롤` + `Easy/Medium/Hard` | 8렙 완성/Fast 9 추정만 | `levels`의 표본 최다 레벨 + `rerolls` 필드로 리롤 여부, 난이도는 metatft 로직(“difficulty”) 재현 | 있음 | 낮음 |
| 1-3 | **특성(시너지) 표시** | 카드 상단에 활성 특성 아이콘 + 단계 수 | 없음 (유닛 특성 텍스트만) | `traits.json`에 아이콘 추가(sync.mjs 한 섹션) → 카드에 `협곡야수 4 · 사냥꾼 2` 아이콘 줄. 특성 단계 임계값은 cdragon `traits[].effects.minUnits` | 있음 | 중간 |
| 1-4 | **3성 목표 표시** | 초상화 위 ★★★ 강조, 리롤 덱 구분 | 별 작게 | 3성 목표 유닛은 별 색·크기 차등, 카드 태그에 "리롤" | 있음 | 낮음 |
| 1-5 | **덱 목록 필터** | 패치·기간·티어(플래티넘+ 등)·큐 | 티어·정렬만 | 패치 전환(`sync:meta`에 cluster 여러 개 저장), 랭크 구간(`ranks` 필드) | 부분 | 중간 |
| 1-6 | **아이템 우선순위** | 캐리템 1→2→3 순서, 대체 아이템 | 상위 빌드 1개 | `builds`에서 유닛별 상위 3빌드 → "1순위 / 대체" 표시, 아이템 장착 유닛 표시(탱커템 포함) | 있음 | 낮음 |
| 1-7 | **덱 상세: 증강** | 덱별 추천 증강 티어 | 없음 | `comp_details.augments` + `comp_augment_tiers` API. 증강 한글명은 cdragon `augments` | 있음 | 중간 |
| 1-8 | **레벨별 보드 = 배치도** | 레벨마다 실제 보드 배치 | 레벨별은 아이콘 줄, 배치도는 최종만 | `positioning`은 최종 기준이라 레벨별 배치는 휴리스틱(현 board.ts) 재사용 → 레벨 탭 넘기면 보드가 채워지는 UI | 부분 | 중간 |
| 1-9 | **덱 이름 품질** | 사람이 지은 이름(고밸류 드레이븐, 개화 Rush 9) | 자동 생성(나무정령 이즈리얼 드레이븐) | 규칙 보강: 캐리 1명 + 핵심 특성 1개 + 리롤/Fast 9 접미. 수동 override 맵(`deck-notes.ts`) | — | 낮음 |
| 1-10 | **데이터 갱신 주기** | 실시간~수 분 | 수동 `sync:meta` | GitHub Actions cron(하루 1회) → 커밋 → Netlify 자동 배포. 푸터에 갱신 시각 | — | 낮음 |
| 1-11 | **모바일 밀도** | 카드 1열, 통계 유지 | 대체로 OK | 티어 리스트 카드 초상화 9개 2줄 → 가로 스크롤 1줄 옵션 | — | 낮음 |

## 2. 추가하면 좋을 부분 (그들에겐 있고 우리엔 없는 기능)

| # | 기능 | 참고 | 설명 | 데이터 | 난이도 |
|---|---|---|---|---|---|
| 2-1 | **팀 코드 복사** | 롤체지지 | 덱을 인게임 팀 플래너로 바로 가져가는 코드. 초반 유저에게 가장 실용적. 코드 포맷은 리버스 필요(롤체지지/metatft가 생성하는 문자열 분석) | 조사 필요 | 중간 |
| 2-2 | **챔피언 페이지** | 셋 다 | 챔피언별 코스트·특성·스킬·추천 아이템·등장 덱. `units.json` + cdragon `ability` 필드 + `unit_items_processed` API | 있음 | 중간 |
| 2-3 | **아이템 페이지** | 셋 다 | 조합표(9×9 그리드)·아이템별 잘 쓰는 챔피언·평균등수. `items.json` + metatft `tft-stat-api/items` | 있음 | 낮음 |
| 2-4 | **증강 티어 리스트** | 셋 다 | 증강별 평균등수·픽률, 스테이지(2-1/3-2/4-2)별. `tft-stat-api/augments_tiers` | 있음 | 중간 |
| 2-5 | **특성(시너지) 페이지** | 롤체지지·OP.GG | 특성 단계별 효과 + 보유 챔피언 + 활성 시 평균등수 | 있음(cdragon) | 낮음 |
| 2-6 | **배치툴 / 덱 빌더** | 셋 다 | 빈 보드에 유닛 드래그 → 특성 계산 → 저장/공유. 우리 배치도 컴포넌트 확장 | — | 높음 |
| 2-7 | **패치 노트 / 버프·너프** | OP.GG | cdragon 두 패치 diff(스탯·스킬 수치) 자동 생성 가능 | 있음 | 중간 |
| 2-8 | **전적 검색** | 셋 다 | Riot API 키·백엔드 필요. 이 서비스 목적과 거리 있음 → **보류 권장** | Riot API | 높음 |
| 2-9 | **덱 상세: 카운터** | MetaTFT | "이 덱을 만나면 등수 +0.35" 같은 상성. `comp_details.counters` | 있음 | 낮음 |
| 2-10 | **덱 상세: 추세 그래프** | MetaTFT | 일자별 평균등수·픽률 스파크라인. `comp_details.trends` | 있음 | 낮음 |
| 2-11 | **최근 1위 덱** | 롤체지지 | 고랭커 실제 1등 보드. `proComps` 필드(팀 빌더 공개 덱)로 유사 구현 | 부분 | 중간 |
| 2-12 | **다크/라이트, 언어** | MetaTFT(EN 등) | 영어 UI 옵션 — cdragon `en_us.json`로 이름만 바꾸면 됨 | 있음 | 낮음 |

## 3. 독자적인 포인트 (그들에게 없는, 우리만의 방향)

세 사이트는 전부 **"덱 카탈로그"** 다. 우리는 **"내 상황 → 결정"** 도구. 이 축을 끝까지 밀어야 한다.

| # | 포인트 | 왜 우리만 가능한가 | 구현 | 데이터 | 난이도 |
|---|---|---|---|---|---|
| 3-1 | **상황 입력 → 덱 추천** (현재 핵심) | 셋 다 없음. MetaTFT는 인게임 앱 설치해야 함 | 이미 있음. 초반 기준 점수 체계 유지·튠 | 있음 | — |
| 3-2 | **분기 트리 (Pivot Tree)** | 같은 초반 라인(오른·자야·바루스)에서 갈라지는 덱들을 **한 화면**에서. "6렙까지 같고 7렙에서 갈린다" | `levels`로 덱 간 레벨별 유닛 교집합 계산 → 추천 결과 상단에 "이 초반이면 A/B/C 중 X렙에서 결정" 트리 뷰 | 있음 | 중간 |
| 3-3 | **1-1 회전목마 가이드** | 게임 시작 첫 선택을 도와주는 곳이 없음 | metatft `first_carousel` (덱별 첫 회전목마 아이템 통계) 집계 → "지금 메타에서 첫 픽 재료 순위" + 그 재료로 갈 수 있는 덱 | 있음 | 낮음 |
| 3-4 | **로비 스카우팅: 경쟁 덱 회피** | 상성/경합은 아무도 입력받지 않음 | 다른 플레이어 보드 유닛 몇 개 입력 → 그들이 가는 덱 추정 → 내 추천에서 **경합(유닛 겹침) 페널티** + `counters`로 상성 반영 | 있음 | 중간 |
| 3-5 | **3성 앵커 규칙** (있음) | 통계 사이트는 유저 3성을 모름 | 이미 있음. UI에서 "3성 X 중심 덱만 보기" 필터로 노출 | — | 낮음 |
| 3-6 | **완성 아이템 = 분해 불가** (있음) | 카탈로그엔 개념 자체가 없음 | 이미 있음. "증강으로 분해 가능" 체크 추가 | — | 낮음 |
| 3-7 | **다음 행동 카드** | "다음 목표 N렙 → 사야 할 유닛" 이미 있음 | 확장: **다음 회전목마에서 집을 재료**(캐리템 레시피 − 보유), **팔아도 되는 유닛**(어느 덱에도 안 쓰임) | 있음 | 낮음 |
| 3-8 | **덱 상세 내 보유 토글** (있음) | 카탈로그는 정적 | 이미 있음. 홈 localStorage와 동기화하면 "라운드마다 갱신" 경험 완성 | — | 낮음 |
| 3-9 | **라운드 진행 모드** | 게임 한 판을 따라가는 UX는 없음 | 스테이지 표시(2-1 → 2-3 → 3-2…) + 각 단계에서 입력 갱신 → 추천 변화 로그. 세션 저장 | — | 중간 |
| 3-10 | **광고 최소·즉시 로딩** | 셋 다 배너 광고 과다, SPA 로딩 느림 | 정적 JSON + SSR, 광고는 사이드 2개만. 성능을 마케팅 포인트로 | — | — |
| 3-11 | **한국어 게임 용어 100%** | 롤체지지·OP.GG는 한글이지만 metatft는 영어 | cdragon ko_kr 기준 유지, 덱 이름 규칙 한국 커뮤니티식(“개화 Rush 9”) | 있음 | 낮음 |

---

## 추천 우선순위 (효과 ÷ 비용)

1. **1-1 통계 4종 + 1-2 태그 + 1-6 아이템 우선순위** — 데이터 이미 받아오고 있음, 카드 신뢰도 즉시 상승
2. **3-3 회전목마 가이드 + 3-7 다음 행동 카드 확장** — 우리 정체성(초반 결정) 강화, 구현 작음
3. **1-3 특성 아이콘** — 카드가 "덱처럼" 보이게 하는 가장 큰 시각 요소
4. **3-2 분기 트리** — 독자 기능 중 가장 설득력 있음
5. **1-10 자동 갱신(GitHub Actions)** — 운영 비용 0으로
6. **2-1 팀 코드** — 유저 체감 큰데 포맷 조사 필요
7. 2-2~2-5 카탈로그 페이지(챔피언·아이템·증강·특성) — SEO 유입용, 데이터는 다 있음
8. 3-4 로비 스카우팅 — 차별화 크지만 입력 UX 설계 필요

보류: 2-6 배치툴(비용 큼, 그들이 이미 잘함), 2-8 전적 검색(목적 이탈, Riot API 필요)
