# Netlify → Cloudflare Pages 완전 이전 가이드

작성 2026-09-17. panlab.lol을 Netlify에서 Cloudflare Pages(OpenNext 어댑터)로 이전하는 절차.

---

## 파트 A — 코드 작업 (Claude Code에게 시킬 것)

아래를 그대로 Claude Code에 지시하세요. 순서대로 진행하고, 각 단계 완료 후 결과를 확인하고 넘어가주세요.

### A1. OpenNext 어댑터 설치 및 로컬 빌드 검증

```bash
npm install @opennextjs/cloudflare
npx wrangler --version   # 없으면 npm install -g wrangler
```

`open-next.config.ts`를 프로젝트 루트에 생성:

```ts
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
```

`wrangler.jsonc` 생성 (또는 `wrangler.toml`):

```jsonc
{
  "name": "panlab",
  "compatibility_date": "2026-09-01",
  "compatibility_flags": ["nodejs_compat"],
  "pages_build_output_dir": ".open-next/assets"
}
```

로컬에서 빌드 테스트:

```bash
npx opennextjs-cloudflare build
npx opennextjs-cloudflare preview
```

빌드 에러가 나면 원인을 파악해서 보고해주세요. 특히 다음을 확인:
- `fs`, `path`, `crypto.createHash` 등 Node 전용 API 사용 여부 (Workers에서 미지원)
- `next/image`의 기본 최적화 로더 사용 여부 (`sharp` 미지원 → `images: { unoptimized: true }` 필요할 수 있음)
- API route에 `export const runtime = 'edge'`가 남아있다면 **제거**하세요 (OpenNext는 Node 런타임을 그대로 쓰므로 불필요하고, 오히려 충돌할 수 있음)

### A2. 정적 페이지 프리렌더 전환

아래 페이지들은 Workers Functions 호출 없이 완전히 정적으로 서빙되도록 `generateStaticParams`를 적용하세요:
- `/decks` (덱 목록)
- `/champions`, `/items`, `/traits` (마스터 데이터 페이지)
- `/deck/[id]` (쿼리 파라미터 없는 기본 페이지)

사용자 상태(보유 아이템/기물 토글)는 URL 쿼리가 아니라 클라이언트 상태(React state 또는 localStorage)로 유지하고, 점수 계산만 `/api/recommend` 호출로 처리되도록 구조를 확인해주세요. 이게 되어 있어야 위 페이지들이 Workers Functions 무료 한도(하루 10만 요청)를 전혀 소모하지 않습니다.

### A3. 환경변수 읽는 방식 점검

Workers는 `process.env`를 빌드 타임에 인라인하지 않고 런타임 바인딩으로 주입합니다. 모듈 최상단에서 환경변수를 읽는 코드가 있다면, **함수/핸들러 안에서 읽도록** 수정하세요:

```ts
// 잘못된 예 (모듈 최상단)
const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, ...);

// 올바른 예 (핸들러 안에서)
export async function POST(req: Request) {
  const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, ...);
  // ...
}
```

### A4. 캐시 로직 옵셔널 처리 확인

`caches.default`(Cache API)는 Workers 환경에서만 존재하고 `next dev`나 로컬 환경엔 없습니다. `/api/recommend`의 번들 캐싱 코드가 이 객체 존재 여부를 안전하게 체크하는지 확인하세요:

```ts
const cache = (globalThis as any).caches?.default as Cache | undefined;
const hit = await cache?.match(cacheKey);   // 없으면 undefined, 에러 안 남
```

### A5. package.json 빌드 스크립트 추가

```json
{
  "scripts": {
    "build:cf": "opennextjs-cloudflare build",
    "preview:cf": "opennextjs-cloudflare preview",
    "deploy:cf": "opennextjs-cloudflare deploy"
  }
}
```

---

## 파트 B — Cloudflare 대시보드에서 직접 할 것

코드 작업(A1~A5)이 끝난 뒤 진행하세요.

### B1. Cloudflare 계정 생성 + Pages 프로젝트 연결

1. dash.cloudflare.com에서 계정 생성 (이미 있으면 스킵)
2. Workers & Pages > Create > Pages > Connect to Git
3. GitHub 저장소(panlab 프로젝트) 연결
4. 빌드 설정:
   - **Build command**: `npm run build:cf`
   - **Build output directory**: `.open-next/assets`
   - **Root directory**: 프로젝트가 모노레포가 아니면 비워둠

### B2. 환경변수/시크릿 등록

Pages 프로젝트 > Settings > Environment variables에서 등록 (Production/Preview 환경 각각):

| 이름 | 값 | 비고 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL | 공개 가능 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Publishable key (`sb_publishable_...`) | 공개 가능 |

`SUPABASE_SERVICE_ROLE_KEY`(Secret key)는 **Pages 프로젝트에 등록하지 마세요** — `/api/recommend`는 읽기만 하므로 anon/publishable 키만 있으면 됩니다. Service role 키는 별도 수집기 스크립트(GitHub Actions 등)에만 필요합니다.

### B3. 첫 배포 및 미리보기 테스트

배포가 끝나면 Cloudflare가 `*.pages.dev` 도메인을 자동으로 줍니다. 이 URL로 먼저 접속해서:
- 정적 페이지(덱 목록 등)가 정상 로드되는지
- `/api/recommend`가 실제로 Supabase 데이터를 가져와 응답하는지
- 이미지가 깨지지 않는지 (최적화 로더 이슈 확인)

전부 확인될 때까지 DNS는 건드리지 마세요.

### B4. 네임서버를 Cloudflare로 전환

1. Cloudflare 대시보드에서 사이트 추가 → `panlab.lol` 입력
2. Cloudflare가 주는 2개의 네임서버 값을 확인
3. Spaceship 도메인 관리 화면(Advanced DNS 옆의 네임서버 설정)에서 기존 `launch1/2.spaceship.net`을 Cloudflare가 준 네임서버로 교체
4. 전파는 몇 분~최대 24시간 소요 (보통 국내는 빠른 편)

### B5. 커스텀 도메인 연결

네임서버 전환이 완료되면 (Cloudflare 대시보드에서 "Active" 상태 확인 후):
1. Pages 프로젝트 > Custom domains > Set up a custom domain
2. `panlab.lol`과 `www.panlab.lol` 각각 추가
3. Cloudflare가 자동으로 SSL 인증서 발급 (몇 분 소요)

### B6. Netlify 정리

모든 게 정상 작동하는 것을 며칠 지켜본 뒤:
- Netlify 프로젝트 삭제 또는 배포 중지
- Netlify 관련 환경변수/웹훅 정리

---

## 체크리스트 요약

- [ ] A1: OpenNext 로컬 빌드 성공
- [ ] A2: 정적 페이지 프리렌더 적용
- [ ] A3: 환경변수 런타임 읽기로 수정
- [ ] A4: 캐시 옵셔널 처리 확인
- [ ] B1: Cloudflare Pages 프로젝트 생성 + 빌드 성공
- [ ] B2: 환경변수 등록 (anon/publishable 키만)
- [ ] B3: `*.pages.dev`에서 기능 전체 테스트
- [ ] B4: 네임서버 Cloudflare로 전환
- [ ] B5: panlab.lol 커스텀 도메인 연결 + SSL 확인
- [ ] B6: Netlify 정리

## 주의사항

- **B4(네임서버 전환) 전에 B3(미리보기 테스트)를 반드시 끝내세요.** 순서를 바꾸면 도메인이 전환된 상태에서 사이트가 안 뜨는 최악의 상황이 생길 수 있습니다.
- Riot API 수집기(별도 스크립트)는 이 이전 작업과 무관하게 계속 별도로 운영됩니다 — Cloudflare Pages는 Next.js 앱만 담당합니다.
